module.exports = {
    theme: {
      extend: {
        fontSize: {
          '2xs': '0.625rem',   // 10px (se não existir no padrão)
          'md':   '1rem'     // 16px (se não existir no padrão)
        },
      },
    },
    plugins: [],
  }
